/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "server-bsnty.vercel.app",
      "res.cloudinary.com",
      "localhost:3000",
    ],
  },
};

export default nextConfig;
