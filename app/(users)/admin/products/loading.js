function Loading() {
  return (
    <div className="fixed top-0 left-0 flex items-center justify-center h-full w-full bg-primary-500">
      <span className="loader h-screen"></span>
    </div>
  );
}

export default Loading;
